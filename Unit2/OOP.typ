La programación orientada a objetos es un paradigma de programación que utiliza "objetos" para 
diseñar aplicaciones y programas de computadora. A diferencia de la programación procedimental, que
se centra en funciones y la lógica para manipular datos, OOP se organiza en torno a los datos, es 
decir, los objetos, y los métodos que operan sobre estos datos.

1. *¿Qué es un Objeto?*
   - Un objeto es una entidad dentro de un programa que tiene un estado y comportamiento asociados.

2. *Características de un Objeto*:
   - *Estado*: Representado por atributos o campos. Define las características del objeto.
   - *Comportamiento*: Representado por métodos. Define lo que el objeto puede hacer.
   - *Encapsulamiento*: Los detalles internos del objeto, como su estado, están ocultos del 
    exterior. Esto se logra mediante el uso de modificadores de acceso que restringen el acceso
    directo a los componentes del objeto.

